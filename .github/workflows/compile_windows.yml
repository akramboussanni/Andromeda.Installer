on:
  workflow_call:
    inputs:
      VERSION:
        required: true
        type: string

jobs:
  compile_windows:
    name: Build Windows Executables
    runs-on: ubuntu-latest
    env:
      PROJECT_PATH: ./Andromeda.Installer.csproj
    steps:
      - uses: actions/checkout@v5
      - name: Setup dotnet
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 9.0.x
      - name: Restore Dependencies
        run: dotnet restore "$PROJECT_PATH"

      - name: Build Release for win-x64
        run: dotnet publish "$PROJECT_PATH" -r win-x64 -p:BuildNumber="${{ github.run_number }}" -p:Version="${{ inputs.VERSION }}"

      - name: Upload Release for win-x64
        uses: actions/upload-artifact@v5
        with:
          name: MelonLoader.Installer
          path: Output/win-x64